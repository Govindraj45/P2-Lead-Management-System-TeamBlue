// This line loads the jest-dom library, which adds extra "matchers" for testing DOM elements.
// For example, it gives us helpers like .toBeInTheDocument() and .toBeDisabled()
// that make it easy to check if an HTML element exists or is in a certain state.
import '@testing-library/jest-dom';

/*
 * FILE SUMMARY:
 * This is the test setup file that runs before all frontend tests.
 * It imports the jest-dom library which adds helpful matchers like toBeInTheDocument(),
 * toBeDisabled(), and toHaveClass() to the test framework. Without this file,
 * those convenient checks would not be available in any of the test files.
 * Every test file in the project benefits from this one-line import.
 */
