apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: lms

resources:
  - namespace.yaml
  - secrets.yaml
  - configmap.yaml
  - sqlserver.yaml
  - redis.yaml
  - api.yaml
  - frontend.yaml
  - ingress.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: lead-management-system
    includeSelectors: false
